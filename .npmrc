# React Native / Expo do not follow symlinks reliably: Metro's resolver, the
# Gradle autolinking scripts, CocoaPods and patch-package all expect a flat
# node_modules. `hoisted` makes pnpm produce exactly that.
node-linker=hoisted
